#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	int r;

	 = is_palindrome("level");
	rintf("%d\n", r);
	 = is_palindrome("redder");
	rintf("%d\n", r);
	 = is_palindrome("test");
	rintf("%d\n", r);
	 = is_palindrome("step on no pets");
	pintf("%d\n", r);
	return (0);
}
